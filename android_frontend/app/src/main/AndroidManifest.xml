<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name="org.example.app.NotesApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.SoftMono">
        <activity
            android:name="org.example.app.ui.SettingsActivity"
            android:label="@string/menu_settings"
            android:exported="false" />
        <activity
            android:name="org.example.app.ui.EditNoteActivity"
            android:label="@string/title_edit_note"
            android:exported="false" />
        <activity
            android:name="org.example.app.ui.MainActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
